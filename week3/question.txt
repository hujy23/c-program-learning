1. 完成用于求一元二次方程ax2+bx+c=0的两个实根的c程序。其中a=1.0，b=8.5，c=4。
利用求根公式
程序如下：
#include <math.h>
#include <stdio.h>//请包含两个库文件
printf(“x1=%f\nx2=%f\n”,x1,x2);//打印函数
请使用sqrt()函数求开方

2.请输入一个小写字母后,输出对应的大写字母.
scanf(“%c”,&a);//输入字符放入变量a的地址
